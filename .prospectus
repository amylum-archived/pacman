item do
  expected do
    url_xpath
    url 'https://projects.archlinux.org/pacman.git/refs/'
    xpath '//*[@id="cgit"]/div[1]/table/tr[7]/td[1]/a'
    regex /^v([\d.]+)$/
  end

  actual do
    git_tag
    regex /^(.*)-\d+$/
  end
end
